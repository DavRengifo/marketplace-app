#version: "3.8"

services:
  redis: # Local Redis for development — production uses Upstash
    image: redis:alpine
    container_name: marketplace_redis
    ports:
      - "6380:6379"
    networks:
      - marketplace_network

networks:
  marketplace_network:
    driver: bridge